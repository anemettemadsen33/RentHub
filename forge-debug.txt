# Copy these commands and run on Forge:

cd /home/forge/renthub-tbj7yxj7.on-forge.com

# Check where we are
pwd
ls -la

# Find the correct directory
ls -la current/ 2>/dev/null || echo "No current symlink"
ls -la releases/ 2>/dev/null || echo "No releases folder"

# If it's a standard setup, artisan should be here:
ls -la artisan

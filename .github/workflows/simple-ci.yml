name: Minimal CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  # Just check that code exists - no dependencies, no building
  check:
    name: Repository Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Check Backend Exists
        run: test -d backend && echo "✅ Backend directory exists"
      
      - name: Check Frontend Exists  
        run: test -d frontend && echo "✅ Frontend directory exists"
      
      - name: List Structure
        run: |
          echo "📁 Project Structure:"
          ls -la
          echo ""
          echo "📁 Backend:"
          ls -la backend | head -10
          echo ""
          echo "📁 Frontend:"
          ls -la frontend | head -10

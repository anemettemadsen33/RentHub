{
    "name": "renthub/monorepo",
    "description": "RentHub monorepo deployment wrapper for Laravel Forge",
    "type": "project",
    "require": {
        "php": "^8.2"
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"echo 'Root composer.json installed. The Laravel application is in the backend directory.\\n';\""
        ],
        "post-update-cmd": [
            "@php -r \"echo 'Root composer.json updated. The Laravel application is in the backend directory.\\n';\""
        ]
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
